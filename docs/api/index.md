# API reference

This section is generated from the Release assemblies and XML documentation comments for
`CfSharp.Native`, `CfSharp`, and `CfSharp.Storage.Sqlite`. The generated pages are not hand-edited.

For conceptual guidance, start with the [documentation home](../index.md). For exact signatures,
ownership, and failure contracts, use the generated type and member pages once the preview docs
pipeline has published them.
